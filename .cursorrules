# Git 提交信息规范

当被要求生成 git 提交信息时，必须严格遵循 Conventional Commits 规范和以下规则。提交信息的 subject 和 body 一律使用中文。

## 1. 格式结构

提交信息必须遵循以下结构：

<type>(<scope>): <subject>

<body>

<footer>

## 2. 标题行规则
- **type**：必须使用以下小写英文类型之一：
  - `feat`：新功能
  - `fix`：修复 bug
  - `docs`：仅文档变更
  - `style`：不影响代码含义的变更（空白、格式化、缺少分号等）
  - `refactor`：既不修 bug 也不加功能的代码调整
  - `perf`：提升性能的变更
  - `test`：新增或修正测试
  - `build`：构建系统或外部依赖变更（如 npm、打包配置）
  - `ci`：CI 配置文件和脚本变更
  - `chore`：其他不改动 src 或测试文件的杂项
  - `revert`：回滚之前的提交
- **scope**：可选。用简短的小写英文名词表示改动范围（如 `auth`、`api`、`ui`）。全局性改动可省略。
- **subject**：
  - 使用中文，以动词开头（如「新增」「修复」「更新」「移除」「优化」）
  - 结尾不加句号
  - 简洁，不超过 50 个字

## 3. 正文规则（可选，但推荐）
- 与标题行之间空一行
- 使用中文
- 说明这次改动「做了什么」和「为什么」，不写「怎么做」
- 每行不超过 72 个字符

## 4. 脚注规则（可选）
- 与正文之间空一行
- 用于声明破坏性变更或关联 Issue
- 破坏性变更：以 `BREAKING CHANGE: <说明>` 开头
- 关联 Issue：使用 `Closes #123`、`Fixes #123` 或 `Resolves #123`

## 5. 示例

✅ 好的示例：
feat(auth): 新增 JWT 令牌自动刷新机制

访问令牌过期时自动刷新，避免用户在长会话中意外退出登录。

Closes #45

fix(api): 处理用户接口返回空值导致的崩溃

✅ 好的简单示例：
docs: 更新 README 安装步骤

❌ 差的示例：
修复了登录页的bug
added new feature
更新文件
